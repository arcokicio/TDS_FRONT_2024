const nome = window.document.getElementById("nome")
const email = window.document.getElementById("email")
const ol = window.document.createElement("ul");
const li = window.document.createElement("li");


function SalvaCadastro(){
    const input = window.document.getElementById("nome, idade");

    nome.push(
        {
            nome: input.nome,
            email: input.email
        }
    );

    }

function*(){
    alunos.forEach((SalvaCadastro)=>{
        li.innerHTML = `${mensagem_confirmacao}, Senhor:${body.nome} acabamos de enviar um email para:${body.email}`
        ol.appendChild(li);
    });
    }
    
    console.log(SalvaCadastro);
    body.appendChild(ul);
    
    